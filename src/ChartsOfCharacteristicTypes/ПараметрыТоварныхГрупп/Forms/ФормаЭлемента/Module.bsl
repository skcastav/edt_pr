
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
Наим = СтрЗаменить(Объект.Наименование," ","");
	Если СокрЛП(Объект.Наименование) <> Наим Тогда
	Сообщить("Наименование параметра не может содержать пробелов или других спец. символов!");
	Отказ = Истина;	
	КонецЕсли; 
КонецПроцедуры
