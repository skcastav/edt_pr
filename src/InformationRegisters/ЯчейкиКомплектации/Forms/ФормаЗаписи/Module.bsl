
&НаСервере
Функция ПолучитьБазовуюЕИ()
Возврат(СокрЛП(Запись.МПЗ.ЕдиницаИзмерения.Наименование));
КонецФункции

&НаКлиенте
Процедура ПриОткрытии(Отказ)
Элементы.ЕИ.Заголовок = ПолучитьБазовуюЕИ();
КонецПроцедуры
