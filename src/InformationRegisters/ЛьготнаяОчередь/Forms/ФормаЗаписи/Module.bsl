
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	Если ЗначениеЗаполнено(Запись.ДатаОкончания) Тогда
		Если Запись.Период > Запись.ДатаОкончания Тогда
		Отказ = Истина;
		Сообщить("Дата окончания не может быть более ранней чем дата начала!");	
		КонецЕсли;
	КонецЕсли;  
КонецПроцедуры
