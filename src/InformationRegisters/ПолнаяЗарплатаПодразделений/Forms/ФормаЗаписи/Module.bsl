
&НаКлиенте
Процедура Показать(Команда)
ЗарплатаПоДокументам = ОбщийМодульВызовСервера.ПолучитьЗарплату(Запись.Подразделение,Запись.Линейка,Запись.Период);
КонецПроцедуры


&НаКлиенте
Процедура ПериодНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
СтандартнаяОбработка = Ложь;
Ф = ПолучитьФорму("ОбщаяФорма.ВыборМесяца");
Результат = Ф.ОткрытьМодально();
	Если Результат <> Неопределено Тогда
	Запись.Период = Результат;
	КонецЕсли; 
КонецПроцедуры

