
&НаСервере
Процедура КнопкаОКНаСервере()
ПростойЛинейки = РегистрыСведений.ПростойЛинейки.СоздатьМенеджерЗаписи();
ПростойЛинейки.Период = ТекущаяДата();
ПростойЛинейки.Линейка = Параметры.Линейка;
ПростойЛинейки.Причина = ПричинаПростоя;
ПростойЛинейки.Примечание = Примечание;
ПростойЛинейки.Остановка = Истина;
ПростойЛинейки.Записать();
КонецПроцедуры

&НаКлиенте
Процедура КнопкаОК(Команда)
	Если ЗначениеЗаполнено(ПричинаПростоя) Тогда
	КнопкаОКНаСервере();
	ЭтаФорма.Закрыть(Истина);
	Иначе
	Сообщить("Выберите причину простоя!");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура КнопкаОтмена(Команда)
ЭтаФорма.Закрыть(Ложь);
КонецПроцедуры
